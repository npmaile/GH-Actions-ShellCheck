on: [push]

jobs:
  test:
      runs-on: ubuntu-latest
      name: shellcheck the shellchecker
      steps:
        - name: check checker
          id: check
          uses: npmaile/GH-Actions-ShellCheck@master
